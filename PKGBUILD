pkgname=man-browsed
pkgver=0.1.0
pkgrel=1
pkgdesc='Daemon for serving man pages over HTML'
arch=('any')
depends=('systemd')
makedepends=('rust')
source=('Cargo.lock'
        'Cargo.toml'
        'src'
        'man-browsed.service')
sha256sums=('SKIP', 'SKIP', 'SKIP', 'SKIP')
prepare() {
}

package() {
}
